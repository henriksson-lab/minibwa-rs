[package]
name = "minibwa-rs"
version = "0.1.0"
edition = "2021"

[lib]
path = "src/lib.rs"

[dependencies]
flate2 = "1"
libc = "0.2"
libm = "0.2"
libsais-rs = { path = "../libsais" }
rayon = "1.10"

[build-dependencies]
cc = "1"

[profile.release]
codegen-units = 1
lto = "thin"
